
struct Vector2 {
    byte x;
    byte y;
}

void exit(int code);
void println(str message, (byte, byte) tpl);

struct Slice {
    nuint size;
    *void data;
}

nuint len([]str slice) {
    var slice = transmute(Slice)slice;
    return slice.size;
}

int main([]str argv) {
    if len(argv) <= 1 {
        return 1;
    }

    println("Hello, World", (12, 1));
    

    return 0;
}

