
enum OpenMode {
    Reading,
    Writing
}

struct Vector2 {
    byte x;
    byte y;
}

void exit(int code);
void println(str message, int k);
(int, int) file_open(str path, OpenMode mode);

nuint len([]str slice) {
    var slice = transmute((nuint, *void))slice;
    return slice.0;
}

int main([]str argv) {
    if len(argv) <= 1 {
        return 1;
    } 

    OpenMode t = .Writing;
    var res = file_open("main.cly", t);
    println("main.cly", res.0);

    return 0;
}

